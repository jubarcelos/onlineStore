import './App.css';
import React, { Component } from 'react';
// import * as api from './services/api';

class App extends Component {
  render() {
    return (
      <div>
        <p> Teste de questão 1 </p>
        {/* { api.getCategories().then((requestJson) => { console.log(requestJson); }) } */}
      </div>
    );
  }
}

export default App;
